using System;

class SquareRoot
{
    static void Main()
    {
        int number = 12345;                   // инициализиране на число 
        double result = Math.Sqrt(number);    // double result заради десетичната запетая //                                             Math.Sqrt корен квадратен
        Console.WriteLine(result);
    } 
}

